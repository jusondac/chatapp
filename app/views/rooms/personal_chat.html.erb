<%= turbo_frame_tag 'message_type' do %>
  <h4 class="font-bold my-2">Obrolan</h4>
  <div class="h-1/2 overflow-y-scroll">
    <%= turbo_stream_from "users" %>
    <div id="users">
      <%= render @users %>
    </div>
  </div>
<% end %>
